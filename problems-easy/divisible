// Challenge 8: Is it Divisible
/*
Write a function isDivisible that accepts two numbers and returns true if the first number is cleanly divisible by the second, and false if not.

Examples:
INPUT: isDivisible(9, 3);
OUTPUT: true

INPUT:isDivisible(9, 4);
OUTPUT:false
*/

// MY SOLUTION
function isDivisible(num1, num2)
{
  if(num1%num2 === 0)
  {
    return true;
  }
  return false;
} // arguments were assumed to NOT be undefined

/* OFFICIAL SOLUTION
function isDivisible(one, two) {
  if (one === undefined || two === undefined) {
    console.log('argument undefined');
    return null;
  }

  if (one % two === 0) {
    return true;
  } else {
    return false;
  }
}
*/
